/* eslint-disable @typescript-eslint/no-require-imports */
const fs=require('fs'),path=require('path'); const root=path.resolve(__dirname,'..'),reports=path.join(root,'reports'); fs.mkdirSync(reports,{recursive:true}); const report={mode:'local-only',message:'No database was queried. Run this plan in a separately approved environment after metadata migration to count adult_private and legacy adult media without printing URLs.'}; fs.writeFileSync(path.join(reports,'adult-media-protection-plan.json'),JSON.stringify(report,null,2)+'\n'); fs.writeFileSync(path.join(reports,'adult-media-protection-plan.md'),'# Adult media protection plan\n\nLocal-only dry run: no database, storage, URLs, files, or secrets were accessed. After the metadata migration, count adult post_media rows with `access_level = public` or null metadata, copy them to protected storage manually, update metadata, then stop serving legacy public URLs.\n'); console.log('Adult media protection plan generated (local-only; no data queried).')
