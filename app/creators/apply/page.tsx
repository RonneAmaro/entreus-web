'use client'
import { useState } from 'react'
export default function CreatorApplyPage(){const [message,setMessage]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/creator-interest',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),category:f.get('category'),message:f.get('message'),acknowledged:f.get('acknowledged')==='on'})});setMessage((await r.json()).message)}return <main className="mx-auto max-w-xl p-6"><h1 className="text-3xl font-black">Interesse no Programa Criadores Fundadores</h1><p>Não garante aprovação, pagamento, renda ou monetização imediata.</p><form onSubmit={submit} className="mt-6 space-y-3"><input name="name" required placeholder="Nome" className="w-full border p-3"/><input name="email" required type="email" placeholder="E-mail" className="w-full border p-3"/><select name="category" required className="w-full border p-3"><option value="">Categoria principal</option><option>Vídeos</option><option>Lives</option><option>Educação</option><option>Humor</option><option>Conteúdo adulto verificado</option><option>Outro</option></select><textarea name="message" required maxLength={1200} placeholder="Mensagem curta" className="w-full border p-3"/><label><input name="acknowledged" type="checkbox" required/> Entendo que não há garantia de pagamento, renda, aprovação ou monetização imediata.</label><button className="block rounded bg-black px-5 py-3 text-white">Enviar interesse</button>{message&&<p>{message}</p>}</form></main>}
